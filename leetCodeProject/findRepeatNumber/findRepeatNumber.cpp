#include <stdio.h>
#include <stdlib.h>
#include <algorithm>
#include <vector>

class Solution {
public:
    int findRepeatNumber(vector<int>& nums) {
        if(nums == nullptr || nums.length <= 0)
        {
            return -1;
        }


    }
};